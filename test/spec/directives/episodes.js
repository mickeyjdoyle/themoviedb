'use strict';

describe('Directive: episodes', function () {

  // load the directive's module
  beforeEach(module('mytodoApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
